name: CI macOS

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Build Jumpy
        run: |
          cd Resources/Deployment/macOS
          chmod +x build_Jumpy_CI.sh
          ./build_Jumpy_CI.sh
